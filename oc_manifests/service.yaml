kind: Service
apiVersion: v1
metadata:
  name: observai-ui-insecure
  namespace: testing-workspace
spec:
  selector:
    app: observai-ui-insecure
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
